[package]
name = "time-tracker-core"
version = "0.1.0"
edition = "2021"

[dependencies]
chrono = "0.4"
rusqlite = { version = "0.32", features = ["bundled"] }
serde = { version = "1", features = ["derive"] }
